package General;

/**
 * Created by vineet on 6/21/14.
 */
public class FastDivision {
  public static int divide(int numerator , int denominator){
    /*
    int mask = 0x1;
    while(denominator <= numerator){
      denominator <<= 1;
      mask <<= 1;
    }
    // Now at this point mask will hold the maximum value of quotient such that
    // numerator <= mask * original denominator
    // such that original denominator * mask >= original denominator * quotient > original denominator * mask/2


    while(mask > 1){
      
    }*/
    return 0;

  }
}
